<!DOCTYPE html>

<html lang="en">
  <%- include('includes/head') %>
  <body>
    <%- include('includes/header') %>

    <main>
      <form action="/edit-post/<%=post.id %>" method="POST">
        <!-- This is where we display the error that will display if the form validation is not adhered to. We Display errors using JavaScript in ejs  -->

        <% error.forEach(e =>{ %>
        <p class="notice"><%= e %></p>

        <% })%>

        <fieldset>
          <legend>Edit Existing Post</legend>
          <label for="title"><small>Title </small></label>
          <input
            type="text"
            value="<%= post.title %>"
            id="title"
            name="title"
            autocomplete="off"
          />

          <label for="body"><small>Content</small></label>
          <textarea name="body" id="body"><%= post.body %></textarea>

          <br />
          <button>Save Chaanges</button>
        </fieldset>
      </form>
    </main>

    <%- include('includes/footer') %>
  </body>
</html>
